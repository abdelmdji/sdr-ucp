{
  "name": "sdr-ucp",
  "image": "mcr.microsoft.com/devcontainers/cpp:1-ubuntu-24.04",
  "features": {
    "ghcr.io/devcontainers/features/python:1": { "version": "3.12" }
  },
  "postCreateCommand": "sudo apt-get update -qq && sudo apt-get install -y -qq cmake ninja-build ccache && pip install --quiet pandas numpy matplotlib scipy",
  "hostRequirements": { "cpus": 4 }
}
